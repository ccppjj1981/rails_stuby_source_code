// Generated by CoffeeScript 1.12.2
(function() {
  jQuery(function() {
    $(".editProductLink").on("ajax:success", function(e, data, status, xhr) {
      console.log("editProductLink");
      $('#alert-content').hide();
      $('#editProductFormModal').modal('show');
      $('#editProductName').val(data['name']);
      $('#editProductDescription').val(data['description']);
      $('#editProductPrice').val(data['price']);
      return $("#editProductForm").attr('action', '/products/' + data['id']);
    });
    $("#editProductForm").on("ajax:success", function(e, data, status, xhr) {
      console.log("editProductForm");
      console.log('#product_' + data['id'] + '_name');
      $('#editProductFormModal').modal('hide');
      $('#product_' + data['id'] + '_name').html(data['name']);
      $('#product_' + data['id'] + '_description').html(data['description']);
      return $('#product_' + data['id'] + '_price').html(data['price']);
    }).on("ajax:error", function(e, xhr, status, error) {
      $('#alert-content').show();
      return $('#alert-content #msg').html(xhr.responseText);
    });
    return $("#newProductForm").on("ajax:success", function(e, data, status, xhr) {
      console.log("newProductLink");
      $('#newProductFormModal').modal('hide');
      return $('#newProductForm')[0].reset();
    });
  });

}).call(this);
